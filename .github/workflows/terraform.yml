name: Demo Terraform CI/CD

on:
  workflow_dispatch:

env:
  TF_VERSION: '1.6.0'

jobs:
  terraform-validate:
    name: Validate Terraform
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}

    - name: Terraform Format Check
      run: terraform fmt -check -recursive terraform/

    - name: Terraform Init (Dev)
      run: |
        cd terraform/envs/dev
        terraform init -backend=false

    - name: Terraform Validate (Dev)
      run: |
        cd terraform/envs/dev
        terraform validate

  terraform-test:
    name: Terraform Tests
    runs-on: ubuntu-latest
    needs: terraform-validate
    steps:
    - uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}

    - name: Terraform Init (for tests)
      run: |
        cd terraform/envs/dev
        terraform init -backend=false

    - name: Run Terraform Tests (demo)
      run: |
        echo "Would run terraform test here"
        # cd terraform
        # terraform test -verbose

  terraform-plan-demo:
    name: Terraform Plan (Demo)
    runs-on: ubuntu-latest
    needs: [terraform-validate, terraform-test]
    steps:
    - uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}

    - name: Terraform Plan (Demo)
      run: |
        echo "Would run terraform plan here"
        # cd terraform/envs/dev
        # terraform plan -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
        # -var="region=${{ secrets.GCP_REGION }}"
